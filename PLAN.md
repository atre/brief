# brief — plan

> **How to run this plan (agent):** read `CLAUDE.md` first; work top-down inside a phase; each open item states *what / why / accept* — do not tick `[x]` until its accept check passes; tests never touch the network or the real cluster (fixtures); `snuff` green = done (Stop hook runs it); append friction to `FEEDBACK.md`; never `git commit`/`push` unless the user says so; ask only when two readings lead to materially different work — otherwise pick the simpler one and say so.

Problem: many repos, many sessions, state scattered across PLAN/STATE/TODO/
FEEDBACK files and git; the hub index drifts; half-done work goes cold
unnoticed; every fresh session re-derives "where was I". brief reads what's
already there and ranks it.

## Phase 1 — radar + handoff (done, v0.1)
- [x] discover repos (roots, one-level containers, umbrella repos, exclude)
- [x] git: dirty/untracked/ahead/behind/branch/recent, doc drift by topology
- [x] docs: description, primary state doc, next extraction, checkbox counts
- [x] FEEDBACK untriaged detection (dated sections vs PLAN touch)
- [x] Claude Code session index (7d count, last) across config dirs
- [x] score + reason tags; radar (fold/quiet), handoff (`--tokens`), `--json`, `--md`
- [x] `--hub`: missing / gone / not-a-repo vs hub CLAUDE.md table
- [x] fixture tests with real git, CI, snuff, skill, hub SessionStart wiring

## Phase 1.5 — sharpen (from first real runs)
- [x] `brief --hub --write`: regenerate only the "no index yet" row + append candidate rows (never rewrite curated descriptions) → files: `src/cli.ts` (`--write` flag, valid only with `--hub`), `src/hub.ts` (pure `applyHubWrite(text: string, diff: HubDiff): string` — appends rows `| <name> | <description or —> |` for `diff.missing` directly above the row whose 2nd cell contains `No index yet`, rebuilds that row's comma list from the current not-described names, leaves every other line byte-identical), `src/index.ts` (write the file, print `hub: +N rows, no-index row updated`), README usage line, `test/brief.test.ts` · accept: fixture table with a curated row for `a` and a `No index yet — explore on demand` row listing `x`, `y`; `applyHubWrite(text, {missing:[repo b], …})` → the `a` row unchanged, a row for `b` inserted before the no-index row, and applying it again returns the same string (idempotent). Same work as the Phase 1.6 `--hub --write` item — implement once here, tick both.
- [x] per-repo `.brief.yaml` overrides: stateDoc, nextHeading, description, ignore — zero-config default kept
- [x] `stale-dirty` age in the tag (`stale-dirty 12d`), and a `--stale <days>` knob
- [x] snuff status: run `snuff --json --changed` for the handoff repo only (opt-in `--gates`), never in the radar → files: `src/cli.ts` (`--gates`, repo command only), `src/collect.ts` (`collectOne(name, path, now, idx, staleDays, {gates?: GateRunner})` where `GateRunner = (cwd) => Promise<{ok: boolean; gates: {name: string; ok: boolean; skipped: string|null}[]} | null>`; default runner = `execFile('snuff', ['--json','--changed'], {cwd, timeout: 60_000})`, missing binary/timeout/parse error → `null`), `src/types.ts` (`Repo.gates?: {ok: boolean; red: string[]} | null`), `src/render.ts` (`renderRepo`: `gates: ✗ lint, test` or `gates: ✓ 3/3` after the `sessions:` line; radar untouched), `test/brief.test.ts` · accept: `collectOne('r', dir, NOW, new Map(), 7, {gates: async () => ({ok:false, gates:[{name:'lint', ok:false, skipped:null},{name:'test', ok:true, skipped:null}]})})` → `repo.gates.red` = `['lint']` and `renderRepo` contains `gates: ✗ lint`; `collect()` never invokes the runner (spy call count 0).
- [x] tally join: last-7d tokens per repo (via `tally --json`) as a column, so "active" has a cost → files: `src/types.ts` (`Repo.tokens7d?: number`), new `src/tokens.ts` (`readTally(): TallyJson | null` = `execFile('tally', ['--json','--since','7d'], {timeout: 20_000})`, null on any failure; `attachTokens(repos, json, home)`: `byProject[].project` (`git/acme`) matched to a repo whose path equals `<home>/<project>`; value = `usage.input + usage.output + usage.cacheCreate`), `src/collect.ts` (call once per `collect`, never per repo), `src/render.ts` (radar info line appends `· 1.6M tok/7d` when defined; `--md` gains a `tok 7d` column; `fmtTokens`: `k`/`M` with one decimal), README table row, `test/brief.test.ts` · accept: `attachTokens([repoAt('/h/git/acme')], {byProject:[{project:'git/acme', usage:{input:1, output:2, cacheCreate:3, cacheRead:99}}]}, '/h')` → `tokens7d === 6`; `renderText` line contains `6 tok/7d`; `fmtTokens(1_576_151) === '1.6M'`. decide: always on when `tally` is on PATH; radar and handoff both show it.
- [x] Documents roots (non-git dirs with STATUS/PLAN): show docs signals without git → files: `src/cli.ts` (`--docs-root <dir>` repeatable; env `BRIEF_DOCS_ROOTS`), `src/discover.ts` (`discover(roots, exclude, docsRoots = [])`: each child dir of a docs root that has any of `STATE_DOCS` becomes a candidate `{name: basename, path, docsOnly: true}`; no `.git` needed), `src/collect.ts` (`git === null` path already handled — `docInfos(path, false)`), `src/render.ts` (radar bits: skip the `commit` segment when `git` is null; tag `docs only`), README, `test/brief.test.ts` · accept: tmp docs root with `Notes/STATUS.md` (no `.git`) → `discover([], [], [root])` → `[{name:'Notes', …}]`; `collectOne` gives `git === null`, `docs[0].file === 'STATUS.md'`; the radar line for it contains no `commit`. decide: opt-in only — scanning `~/git` is unchanged, `~/Documents/*` roots come from the hub hook.

## Phase 1.6 — wiring with the other tools (2026-08-16 hub deep-think, ranked)
- [x] **gates column** — read `~/.snuff/<repo-slug>.json` (snuff last result) → `gates ✗ lint (2h)` tag + score (+4 red, +2 stale > 3d) → files: `src/types.ts` (`Repo.gates?: {ok: boolean; red: string[]; ts: number} | null`), new `src/gates.ts` (`readSnuffLast(repoPath, home = process.env.BRIEF_SNUFF_HOME ?? join(homedir(), '.snuff'))`: file `<home>/<slugFor(repoPath)>.json` (reuse `slugFor` from `sessions.ts` — snuff writes the same slug), shape `{ts, ok, gates:[{name, ok, skipped}]}`; missing/unparseable → null), `src/collect.ts`, `src/score.ts` (red → +4 tag `gates ✗ lint (2h)`; ts older than 3 d → +2 tag `gates stale 5d`), `src/render.ts` (handoff `gates:` line), README score table, `test/brief.test.ts` · accept: fixture `<tmp>/<slug>.json` = `{ts: NOW-2h, ok:false, gates:[{name:'lint', ok:false, skipped:null},{name:'test', ok:true, skipped:null}]}` → `score().reasons` includes `gates ✗ lint (2h)` and score is +4; `{ts: NOW-4d, ok:true, gates:[…]}` → reason `gates stale 4d`, +2; no file → no tag. Depends on snuff's "Last-result file" item writing that path.
- [x] **dead CLAUDE.md paths** — grep backticked paths/commands in each repo's CLAUDE.md, report missing (`CLAUDE.md: 3 dead paths`); score +1 each ≤ 5. Every session pays for drift here. → files: `src/docs.ts` (`deadPaths(dir, text): string[]` — backticked tokens matching `^[\w./-]+$` that contain `/` or end in `.md|.ts|.js|.json|.yaml|.yml|.py|.toml|.sh`, no glob chars/`~`/`http`/`<`; resolved against `dir`, `existsSync` false; deduped, cap 5), `src/types.ts` (`Repo.deadPaths: string[]`), `src/collect.ts`, `src/score.ts` (+1 each ≤ 5, tag `CLAUDE.md: N dead paths`), `src/render.ts` (handoff line `dead paths: docs/gone.md`), README score table, `test/brief.test.ts` · accept: CLAUDE.md `` see `src/x.ts` and `docs/gone.md`, run `npm test` `` with only `src/x.ts` present → `deadPaths` → `['docs/gone.md']`; `collectOne` reasons include `CLAUDE.md: 1 dead paths`, score +1; `npm test` and `src/**` are never reported.
- [x] `--brief`/`--top 3` hook mode (≤ 15 lines) — hub SessionStart budget is ~60 lines across brief/tally/pulse → files: `src/cli.ts` (`--brief` → radar with `top: 3` unless `--top` given), `src/render.ts` (`renderText(rep, {top, all, brief?: true})`: header + 2 lines per repo (drop the `↳ next` line), no `below the fold`/`quiet` lines, hard cap 15 lines ending `… +N more, run brief`), README Wiring (hub hook becomes `brief --brief`), `test/brief.test.ts` · accept: `renderText(reportWith20Repos, {top: 3, all: false, brief: true}).split('\n').length <= 15`, no line starts with `below the fold` or `quiet:`; `parseArgs(['--brief'], home).top === 3` and `parseArgs(['--brief','--top','5'], home).top === 5`.
- [x] `--hub --write` restricted to the "no index yet" row + appended candidate rows; curated descriptions never rewritten → files: same as the Phase 1.5 `brief --hub --write` item (`src/hub.ts` `applyHubWrite`, `src/cli.ts`, `src/index.ts`, `test/brief.test.ts`) · accept: that item's idempotent `applyHubWrite` test; tick both when it passes. decide: implemented once under Phase 1.5.
- [x] `pulse` join: if `pulse --json` snapshot exists, show `runtime ✗` for repos that map to a deployment/site (via `.brief.yaml` `service:`) → files: `src/config.ts` (`RepoConfig.service?: string`, `service:` key in `.brief.yaml`), new `src/runtime.ts` (`readPulseLast(home = process.env.BRIEF_PULSE_HOME ?? join(homedir(), '.pulse'))` → `<home>/snaps/last.json` `{ts, findings:[{id, severity}]}` or null; `runtimeFor(service, findings)`: findings whose id is `k8s:<service>`, `cron:<service>`, `site:<service>` or starts with one of them + `:` → worst severity or `'ok'` when none match), `src/types.ts` (`Repo.runtime?: 'crit'|'warn'|'ok'`), `src/collect.ts`, `src/score.ts` (crit +6 tag `runtime ✗`, warn +2 tag `runtime ⚠`), `src/render.ts` (handoff `runtime:` line), README, `test/brief.test.ts` · accept: `.brief.yaml` `service: app/svc-a` + snap `{ts: NOW, findings:[{id:'k8s:app/svc-a', severity:'crit'}]}` → reasons include `runtime ✗`, score +6; no snap file → `runtime` undefined and no tag. Reads what pulse Phase 2.3 writes; until then the reader returns null.

- [x] **PLAN progress on the radar** — what: for repos whose PLAN.md has checkboxes show `PLAN 9/18` in the info line and use the first unticked task (in file order, skipping "- [ ] done" placeholders) as `↳ next` when no state doc gives one; why: brief becomes the queue for agent-run plans; accept: fixture repo with 3 open/2 done → `PLAN 2/5` rendered, `↳ next` = first open item; test in `test/brief.test.ts`.
- [x] `brief feedback --lessons` — what: list FEEDBACK lines prefixed `- lesson:` (any section, triaged or not) with repo + date; `--md` writes a table ready to paste into `~/git/hub/LESSONS.md`; why: general engineering lessons currently die inside one tool's FEEDBACK; accept: fixture FEEDBACK with 2 lesson lines → both listed with repo/date; non-lesson bullets excluded.

## Phase 1.7 — handoff intent + queue (2026-08-16 hub deep-think #2, ranked)
- [x] **Last session's closing words in the handoff** — what: `brief <repo>` prints `last session said: <final assistant text, ≤ 300 chars, whitespace-collapsed>` + `(<ago>, <turns> turns)` from the newest transcript in the repo's session index; why: git/docs reconstruct *state*, not *intent* — "where we stopped and why" dies with the session, and STATE.md hand-maintenance is what brief was meant to make optional; → files: `src/sessions.ts` (`lastAssistantText(file): string | null` — stream the jsonl backwards or read the tail 64 KB, take the last record with `type === 'assistant'` whose content has a `text` block; skip tool_use-only records; return `null` on parse errors), `src/types.ts` (`Repo.lastSaid?: {text: string; ts: number}`), `src/collect.ts` (handoff only, never the radar — one file read per `brief <repo>`), `src/render.ts` (`renderRepo`: line `last session said: …` after `sessions:`; `--tokens` trims it to 120 chars before dropping it), README handoff block, `test/brief.test.ts` (fixture jsonl with tool_use record last + assistant text before it) · accept: fixture session whose last assistant record is a tool_use and the one before says `Stopped at step 3; lint still red` → `lastAssistantText` returns that text; `renderRepo` contains `last session said: Stopped at step 3`; a session file with no assistant text → line absent. decide: 64 KB tail read, no full parse — sessions can be 100 MB.
- [x] **`brief <repo> --next`** — what: print the full text of the repo's next unticked PLAN item (the one `↳ next` points at) and nothing else; why: tally shows `Read PLAN.md` at 13.8k tokens ×3 in one session — agents open the whole plan to find one item; the radar already knows which line it is; → files: `src/cli.ts` (`--next`, repo command only), `src/docs.ts` (`nextItem(text): {line: number; text: string} | null` — first `- [ ]` not matching `/^- \[ \] done$/i`, plus continuation lines indented ≥ 2 spaces), `src/index.ts` (print `PLAN.md:<line>` header + item text, exit 0; no plan/no open item → `no open PLAN item`, exit 1), README, `test/brief.test.ts` · accept: fixture PLAN with two done items, one open multi-line item → `--next` output starts with `PLAN.md:7` and contains the item's second line; repo without PLAN → exit 1. decide: `.brief.yaml stateDoc:` wins over PLAN.md when set.
- [x] **`brief queue`** — what: across all repos, one line per repo whose PLAN.md/STATE.md carries the "How to run this plan (agent)" marker (or `.brief.yaml agentRunnable: true`): `<repo>  PLAN 9/18  ↳ <next item title ≤ 80>`; `--json` = `[{repo, path, planFile, line, done, open, next}]`; why: brief becomes the input for unattended runs (`/gameplan` executor, `claude -p`, an internal task-runner) — the queue exists only in heads today; → files: `src/cli.ts` (subcommand `queue`), `src/docs.ts` (`isAgentRunnable(text)` = `/How to run this plan/i`), `src/render.ts` (`renderQueue`), `src/index.ts`, README, `test/brief.test.ts` · accept: fixture with one agent-runnable plan (2 open) and one plain TODO.md → `queue` lists exactly the first, `--json[0].open === 2`. Depends on the "PLAN progress on the radar" item for the counts — implement that first.
- [x] `unpushed 4d` age in the tag (oldest unpushed commit's author date), so 16 unpushed repos sort by how long the work has been local, and `--md` gets an `unpushed since` column → files: `src/git.ts` (`git log @{u}..HEAD --format=%at | tail -1`), `src/types.ts`, `src/score.ts` (+1 when > 3 d, tag `unpushed 4d`), `src/render.ts`, `test/brief.test.ts` (fixture repo with a remote and 1 unpushed commit dated NOW-4d) · accept: reasons include `unpushed 4d`; radar line shows `1 unpushed (4d)`.
- [x] `--brief` implies the ≤ 15-line hook mode AND is the only thing the hub SessionStart runs — after it ships, point `~/git/hub/.claude/settings.json` at `brief --brief` (by hand, not in this repo) and re-measure with tally's hook-output bucket. (Tracks the Phase 1.6 `--brief` item; listed here so the wiring step is not forgotten.)

## Phase 2 — close the loops
- [x] `brief feedback`: untriaged FEEDBACK sections across all repos with a one-line preview; `## … triage` marker + same-day rule → the consumer the FEEDBACK loop lacked
- [x] `brief init [dir]` (snuff pattern): per-repo SessionStart hook `brief . --tokens 800` — makes STATE.md hand-maintenance optional
- [x] `brief snap` / `brief diff` (squirt pattern): what changed across the workspace since last snapshot — for the Monday view → files: `src/cli.ts` (subcommands `snap [name]`, `diff [name]`, default name `last`), new `src/snap.ts` (`writeSnap(rep, name, home = process.env.BRIEF_HOME ?? join(homedir(), '.brief'))` → `<home>/snaps/<name>.json` `{ts, repos:[{name, score, reasons, dirty, ahead, lastCommitTs, next}]}`; `diffSnaps(prev, cur): {new: string[]; gone: string[]; changed: {name, from, to, reasons}[]; unchanged: number}`), `src/render.ts` (`renderDiff(d, prevTs, now)`: `brief diff — since 3d: 1 new · 1 gone · 1 changed · 30 unchanged` then one line per changed repo `x   5 → 12  +7  2 unpushed`), `src/index.ts`, README usage lines, `test/brief.test.ts` · accept: `diffSnaps(snapWith(x:5, y:1), snapWith(x:12, z:3))` → `{new:['z'], gone:['y'], changed:[{name:'x', from:5, to:12, …}], unchanged:0}`; `renderDiff` first line contains `1 new · 1 gone · 1 changed`; `brief snap` writes under `~/.brief` (Non-goals allow it).
- [x] weekly `brief --md > reports/YYYY-WW.md` in this repo (workspace snapshot for the Monday view); no external targets → files: `package.json` (script `report`: `mkdir -p reports && node dist/index.js --md > reports/$(date +%G-W%V).md`), `reports/` (new dir; add `reports/.gitkeep`), README Wiring row · accept: `npm run report` creates `reports/2026-W33.md` (today) whose first line is `# brief`. decide: no scheduler in code — invoked by hand or the `/weekly` skill; `reports/*.md` is gitignored (workspace-wide data), only `.gitkeep` is tracked.

## Fleet review 2026-08-17 (hub TOOLS.md Round 4) — this section is the queue

Verdict: **BUILT, NOT USED** (0 real invocations / 30d; hub SessionStart line read by 1 real session; no snaps ever). Every 0.2.0 accept passes; the radar itself is sound — the entry point is the problem, not the tool.
- [x] **Be where sessions start** — resolved itself, not via either hook option below: `tally tools --since 30d` on 2026-08-18 (1 day later) already shows brief at 25 calls / 9 projects, driven by the `/start` and `/brief` **skills** calling `brief <repo> --tokens 900` from Bash, not the hub hook (still hub-only, unchanged). Skip `brief init`/global-hook work — the skill path already puts brief in front of real sessions across several active repos. Re-check with `tally tools` again in a couple weeks to confirm it sticks, but no code/config change needed.
- [ ] **`.brief.yaml service:` beyond app-a** — app-a ✓ and app-c ✓ already had it (not just app-a as this line implied); added worker-b ✓ (`service: worker-b/svc-1`, 2026-08-18). Two open sub-cases surfaced on inspection, not simple copies of the app-c pattern: (a) **done 2026-08-18** — worker-b actually owns 1 k8s + ~13 `cron:worker-b/job-*` pulse ids; `runtimeFor` now accepts a comma-separated `service:` list with trailing-`*` prefix matching (worst severity across all matches wins) — `worker-b/.brief.yaml` is now `service: worker-b/svc-1, worker-b/job-*`, verified live with `brief worker-b` (`runtime: ✓`) and a fixture test in `test/brief.test.ts`; (b) infra-d has **no** `k8s:`/`cron:`/`site:` pulse id at all (only `node:`/`disk:`/`host:`, which `src/runtime.ts` doesn't read by design) — adding `service:` there is a silent no-op, blocked on deciding whether runtime.ts should grow those id kinds. Still open, unresolved. `pull`
- [ ] **`/triage` = `brief feedback --lessons --md >> ~/git/hub/LESSONS.md`** — confirmed still open on inspection: `~/git/hub/LESSONS.md` is hand-curated prose, `--lessons --md` unused by the skill. `dup → merge`
- [ ] **acme `nextHeading` override went stale** — from FEEDBACK 2026-08-16 ("acme next is thin"), re-verified live 2026-08-18: STATE.md was restructured to dated `## Session N — …` headings, no `## open` section exists any more, so `.brief.yaml nextHeading: open` matches nothing and `brief acme` now prints an *empty* `next (STATE.md):` line — worse than the original "thin prose fallback" complaint, not the fix Phase 1.5's per-repo override was supposed to be. Point the override at the latest `## Session` heading (or drop `nextHeading` and let prose-fallback run), verify with a live `brief acme`. Root cause is generic — any `nextHeading:` override silently breaks when the doc's heading convention changes and brief has no way to flag it (quiet-wrong is fine for *no* next, less fine for a previously-working one going silently empty) — worth a fixture test for "configured heading no longer present" once fixed. `pull`
- [ ] **`brief feedback` untriaged watermark is wrong: PLAN.md mtime, not a triage marker** — found 2026-08-18 dogfooding from `~/git/pulse`, then reproduced against this repo's own `feedback --only brief` call same day. `feedbackInfo`'s "same-day counts only if FEEDBACK.md is newer than PLAN.md" rule means *any* PLAN.md touch — for any reason, unrelated content included — retroactively marks that whole day's FEEDBACK section reviewed, even if nothing in it was read. Silent false-negative, opposite of the tool's "wrong quietly, never loudly" policy (this is loud-wrong: it says "0 untriaged" when there's real unread content). Fix: watermark off the actual `` ## <date> — triage `` marker present in FEEDBACK.md (per-section, like the existing marker-detection already does for skipping *triaged* sections) instead of comparing file mtimes against PLAN.md. → files: `src/docs.ts` (`feedbackInfo`), `test/brief.test.ts` (fixture: PLAN.md touched same day as an unreviewed FEEDBACK section with no triage marker → still reported untriaged) · accept: the reproduction case above no longer false-negatives. `gold` — undermines every other item on this list; anything triaged via the mtime rule since 2026-08-16 needs a manual re-check once fixed (this session's brief/pulse triage passes were manual, unaffected).
- [x] **`dormant` tag — years-old dirty repos no longer compete with active work** — found 2026-08-18 (hub session): a years-dormant private repo (13 dirty files, no commit/session in 2.8y) scored 13, same tier as this week's actually-worked repos (looksy/peep 25). `score.ts`: added `dormant` = no commit AND no session in 180d; when true, dirty-file contribution caps at a flat +2 (was `Math.min(changed, 20)`) and the `stale-dirty` bonus is skipped entirely (a multi-year-dormant repo isn't "went cold recently"); reason tag becomes `N dirty, dormant Mmo` instead of a bare `N dirty`. → files: `src/score.ts`, `test/brief.test.ts` (dormant vs. otherwise-identical active-repo fixture, both score paths asserted), `src/cli.ts` help text. 38/38 tests, snuff green.
Park: `snap`/`diff` (no Monday habit yet — `~/.brief/snaps` still doesn't exist), `--hub --write` (never run for real), `queue` (consumer `/gameplan run` doesn't exist). Sunset watch 2026-09-15 for those three.

## Non-goals
- No writes into repos, no server, no DB, no UI. If it needs state, it's a snapshot file under `~/.brief`.
